{
  "name": "e1m-spec",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for the E1M™ pinout standard. The package itself is not published; only the spec content (STANDARD.md, pinout/*.json) is normative.",
  "license": "CC-BY-SA-4.0",
  "scripts": {
    "validate": "npm run validate:e1m && npm run validate:e1m-x",
    "validate:e1m": "ajv validate --spec=draft2020 -s pinout/schema/loom-v1.schema.json -d pinout/v1.json --strict=true --all-errors",
    "validate:e1m-x": "ajv validate --spec=draft2020 -s pinout/schema/loom-v1.schema.json -d pinout/x-v1.json --strict=true --all-errors"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1"
  }
}
